[profile.default]
src = 'src'
out = 'out'
libs = ['lib']
solc_version = '0.8.13'

[fmt]
line_length = 120
tab_width = 2

[rpc_endpoints]
bsc = "${BSC_RPC_URL}"

[etherscan]
bsc = { key = "${BSC_ETHERSCAN_KEY}", chain = 56, url = "https://api.bscscan.com/api" }
